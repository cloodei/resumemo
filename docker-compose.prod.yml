services:
  nginx:
    image: nginx:mainline-alpine-slim
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "80:80"
    volumes:
      - ./deploy/nginx/api.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
    logging:
      options:
        max-size: "10m"
        max-file: "3"

  api:
    image: ghcr.io/${GHCR_OWNER}/resumemo-api:${API_IMAGE_TAG}
    env_file:
      - ./env/api.env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    logging:
      options:
        max-size: "10m"
        max-file: "3"

  pipeline:
    image: ghcr.io/${GHCR_OWNER}/resumemo-pipeline:${PIPELINE_IMAGE_TAG}
    env_file:
      - ./env/pipeline.env
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    logging:
      options:
        max-size: "10m"
        max-file: "3"
