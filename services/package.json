{
  "name": "services",
  "private": true,
  "scripts": {
    "down": "cd pipeline && bun run down",
    "sync": "cd pipeline && bun run sync",
    "dev": "cd pipeline && bun run dev",
    "start": "bun dev"
  }
}
